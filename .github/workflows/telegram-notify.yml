name: Smart Git Changes Notifier
on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Получаем текущий и предыдущий коммиты

      - name: Analyze changes
        id: analyze
        run: |
          # Получаем список измененных файлов с типами изменений
          CHANGES=$(git diff --name-status HEAD^ HEAD | while read status file; do
            case $status in
              A) echo "• 🆕 Добавлен: $file";;
              M) echo "• ✏️ Изменен: $file";;
              D) echo "• ❌ Удален: $file";;
              R*) echo "• ♻️ Переименован: ${file#*$'\t'}";;
              C) echo "• ©️ Скопирован: $file";;
              *) echo "• 🔄 Модифицирован: $file";;
            esac
          done)

          # Анализируем изменения в каждом файле
          DETAILS=$(git diff --stat HEAD^ HEAD)
          COMMIT_MSG=$(git log -1 --pretty=format:'%s')
          AUTHOR=$(git log -1 --pretty=format:'%an')
          DATE=$(git log -1 --pretty=format:'%cd' --date=format:'%Y-%m-%d %H:%M')

          # Сохраняем результаты
          echo "CHANGES<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "DETAILS<<EOF" >> $GITHUB_OUTPUT
          echo "$DETAILS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "COMMIT_MSG=$COMMIT_MSG" >> $GITHUB_OUTPUT
          echo "AUTHOR=$AUTHOR" >> $GITHUB_OUTPUT
          echo "DATE=$DATE" >> $GITHUB_OUTPUT

      - name: Compose message
        run: |
          # Формируем читаемое сообщение
          MESSAGE="📌 *Обновление в проекте*: ${GITHUB_REPOSITORY##*/}
          
          📝 *Коммит*: ${{ steps.analyze.outputs.COMMIT_MSG }}
          👤 *Автор*: ${{ steps.analyze.outputs.AUTHOR }}
          📅 *Дата*: ${{ steps.analyze.outputs.DATE }}
          
          📂 *Список изменений*:
          ${{ steps.analyze.outputs.CHANGES }}
          
          📊 *Детали*:
          \`\`\`
          ${{ steps.analyze.outputs.DETAILS }}
          \`\`\`
          
          🔗 *Полные изменения*: 
          https://github.com/$GITHUB_REPOSITORY/commit/$GITHUB_SHA"

          echo "MESSAGE<<EOF" >> $GITHUB_ENV
          echo "$MESSAGE" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Send to Telegram
        run: |
          curl -X POST \
          -H "Content-Type: application/json" \
          -d '{
            "chat_id": "5615087670",
            "text": "'"$MESSAGE"'",
            "parse_mode": "MarkdownV2",
            "disable_web_page_preview": true
          }' \
          "https://api.telegram.org/bot7715307360:AAFvEpydlOSPY_pGT51AbExRAaECg-1P06Q/sendMessage"
